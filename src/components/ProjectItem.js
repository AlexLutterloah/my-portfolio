import Container from "@mui/material/Container";

const ProjectItem = () => {
    return (
        <Container
            maxWidth="xs"
            sx={{ border: 2, borderColor: "white", height: 200, mt: 5 }}
        >
            Some content
        </Container>
    );
};

export default ProjectItem;
